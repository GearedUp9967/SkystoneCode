package org.firstinspires.ftc.teamcode;

import com.qualcomm.robotcore.eventloop.opmode.OpMode;
import com.qualcomm.robotcore.eventloop.opmode.TeleOp;

/*
@TeleOp(name="JARVIS")
public class JARVIS extends OpMode {
    HardwareJARVIS robot=new HardwareJARVIS();
    double speed=1;
    Button a=new Button();
    Button b=new Button();

    @Override
    public void init() {
        robot.init(hardwareMap);
    }

    @Override
    public void loop() {
        a.update(gamepad1.a);
        b.update(gamepad1.b);
        if (gamepad1.left_bumper) {
            robot.turnerMove(0.05);
        }
        else if (gamepad1.right_bumper) {
            robot.turnerMove(-0.05);
        }
        if (gamepad1.a) {
            robot.Forward(-.5);
        }
        else if (gamepad1.b) {
            robot.Forward(.5);
        }
        else
            robot.Forward(0);
*/



