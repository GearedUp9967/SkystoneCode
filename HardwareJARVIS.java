package org.firstinspires.ftc.teamcode;
/*
import com.qualcomm.robotcore.hardware.DcMotor;
import com.qualcomm.robotcore.hardware.HardwareMap;
import com.qualcomm.robotcore.hardware.Servo;
import com.qualcomm.robotcore.util.Hardware;

public class HardwareJARVIS {
    public DcMotor right;
    public DcMotor left;
    public Servo turner;

    public double turnerPos=;

    HardwareMap hwMap;



  public double TURNER_DOWN=;

    public void init(HardwareMap ahwMap) {
        hwMap=ahwMap;
        right = hwMap.dcMotor.get("right");
        left = hwMap.dcMotor.get("right");
        left.setZeroPowerBehavior(DcMotor.ZeroPowerBehavior.BRAKE);
        right.setZeroPowerBehavior(DcMotor.ZeroPowerBehavior.BRAKE);
        turner.setPosition(TURNER_DOWN);
    }
    public void powerForward(double power){
        right.setPower(-power);
        left.setPower(power);}
    public void turnerMove(double dist)
    {
      if(turnerPos+dist>TURNER_DOWN && turnerPos.dist>1)
      {
          turnerPos=turnerPos+dist;
      }
      turner.setPosition(turnerPos);
    }

}
*/